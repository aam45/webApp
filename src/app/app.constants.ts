export enum APP_ROUTES {
  HOME = 'home',
  ABOUT = 'about',
  PORTFOLIO = 'portfolio',
  BLOGS = 'blogs',
  CONTACT = 'contact',
  BLOGS_DETAILS = 'blogs/details',
  NOT_FOUND = '**',
}

export type APP_STEPS = APP_ROUTES;
